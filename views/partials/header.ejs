<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CampLife!!</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.1/css/all.css">
    <link rel="stylesheet" href="/stylesheets/main.css">
</head>
<body>

  <nav id="navid" class="navbar navbar-expand-lg navbar-dark bg-dark mb-5">
    <a class="navbar-brand" href="/landing">CampLife</a>
    <button class="navbar-toggler" data-toggle="collapse" data-target="#navlink"  aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <!--For the Home icon-->
    <div class="collapse navbar-collapse">   <!--These will collapse under Hamberger icon-->
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a style="font-size:1.2rem" class="nav-link active pb-1" href="/campgrounds">Home</a>
        </li>
      </ul>
    </div>

    <div class="collapse navbar-collapse justify-content-end" id="navlink">  <!--These will collapse under Hamberger icon-->
      <ul class="navbar-nav">
        <% if(!currentUser){ %>
          <li class="nav-item active">
            <a class="nav-link" href="/login">Login</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="/register">SignUp</a>
          </li>
        <% }else{%>
          <li><a href="#" class="nav nav-link active">Signed in as <%=currentUser.username%></a></li>
          <li class="nav-item active">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
        <%}%>
      </ul>
    </div>
  </nav>

  <div class="container">
    <% if(error && error.length>0){ %> <!-- As empty error is also a error so error.length>0 is used -->
    <div class="alert alert-danger" role="alert">
      <%=error%>
      <!-- For the close icon-->
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
  </div>
  <% } %>
    <% if(success && success.length>0){ %>
    <div class="alert alert-success" role="alert">
        <%=success%>
        <!-- For the close icon-->
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
      </div>
    <% } %>
  </div>